<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.creditservice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MessageActivity"></activity>
        <activity android:name=".SendMessageActivity" />
        <activity android:name=".CreditPageActivity" />
        <activity android:name=".MainBankActivity" />
        <activity android:name=".RegistrationBankActivity" />
        <activity android:name=".ProfileFillActivity" />
        <activity android:name=".RegistrationUserActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".MainUserActivity" />
        <activity android:name=".BankSignIn" />
        <activity android:name=".UserSignIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>